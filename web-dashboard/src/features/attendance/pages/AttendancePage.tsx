import { ComingSoonPage } from '@/components/layout/ComingSoonPage'

export function AttendancePage() {
  return (
    <ComingSoonPage
      backendNote="Các model attendance đã có trong backend, nhưng router attendance chưa được include dưới /api/v1."
      title="Chấm công"
    />
  )
}
